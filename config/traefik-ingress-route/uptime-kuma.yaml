---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: uptime-kuma
  namespace: monitoring
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`uptime.localhost`)
      kind: Rule
      services:
        - name: uptime-kuma
          port: 3001

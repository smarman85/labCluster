---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: test-pr
  namespace: test
spec:
  values:
    replicaCount: 1
    containers:
    - name: test
      image: somemadeupregistry.something.io/info/repo-name:v.0.0.1 # {"$imagepolicy": "namespace:repo-name-image-policy"}
      ports: [8080]
      env:
        DONT: needone
